{% extends "layouts/_base.njk" %}

{% block main_content %}

  {% if trails %}
    <ul>
      {% for trail in trails %}
        <li>{{ trail.title }}</li>
      {% endfor %}
    </ul>
  {% else %}
    <h1>No trails found</h1>
  {% endif %}

{% endblock %}
