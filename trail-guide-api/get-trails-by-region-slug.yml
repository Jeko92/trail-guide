info:
  name: get-trails-by-region-slug
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/regions/bavarian-alps/trails
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
