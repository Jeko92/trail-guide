info:
  name: get-trails-by-filters
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/api/trails?maxDistance=9
  params:
    - name: maxDistance
      value: "9"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
