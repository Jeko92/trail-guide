info:
  name: get-trails-by-difficulty
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/api/trails?difficulty=easy
  params:
    - name: difficulty
      value: easy
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
