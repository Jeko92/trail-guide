info:
  name: get-trails-by-region-name
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/api/trails?difficulty=easy&region=bavarian-alps
  params:
    - name: difficulty
      value: easy
      type: query
    - name: region
      value: bavarian-alps
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
